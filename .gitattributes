# Must stay LF - these run on the Linux VPS, and a CRLF shebang line
# breaks the interpreter ("bad interpreter" or silent failures).
*.sh text eol=lf
Dockerfile* text eol=lf
docker-compose.yml text eol=lf
